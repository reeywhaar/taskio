# Plans, scratch scripts, local test data — mine, not the repository's.
private

node_modules

# The built bundle. taskio reads it from disk at startup rather than embedding it, so a clone
# with no dist/ compiles and runs — it serves the placeholder page until Node has run once.
web/dist

# A local instance's database, and the WAL sidecars beside it.
data/
*.db
*.db-wal
*.db-shm

# A local build without -o.
/taskio

.DS_Store
